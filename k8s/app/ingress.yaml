apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: session-app-ingress
spec:
  rules:
    - host: session.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: session-app-service
                port:
                  number: 80